/*
 *                     jack public domain compiler
 *
 *                      15 may MMXXI PUBLIC DOMAIN
 *           The author disclaims copyright to this source code.
 *
 *
 */


class UnitMath {
	function boolean isOk(int l) {
		var int i;
		var boolean ok;
		let ok = false;
		let i = Math.sqrt(25);
		if (i = 5) {
			let ok = true;
		}
		//do Output.printInt(i);
		do Unit.log(l + 1, "Math.sqrt...", ok);
		let ok = false;
		let i = Math.divide(25, 2);
		if (i = 12) {
			let ok = true;
		}
		do Unit.log(l + 1, "Math.divide...", ok);

		let ok = false;
		let i = Math.multiply(5, 5);
		if (i = 25) {
			let ok = true;
		}
		do Unit.log(l + 1, "Math.multyply...", ok);
	

		return Memory.checkEmpty();
	}
	
}

